export const selectRoutesPoints = (state) => {
  return state.routesPoints.routesPoints;
};

export const selectRoutesPointsStatus = (state) => {
  return state.routesPoints.status;
};

export const selectRoutes = (state) => {
  return state.routesPoints.routes;
};
